{
  "name": "create-svelte-design",
  "version": "1.0.8",
  "private": true,
  "description": "create sevelte design template",
  "main": "index.js",
  "scripts": {
    "build": "pnpm -r --filter=./packages/** run build",
    "bumpx": "bumpx -s",
    "release": "pnpm bumpx && pnpm publish -r --no-git-checks"
  },
  "keywords": [
    "svelte",
    "svelte-design",
    "svelte-cli",
    "create-svelte"
  ],
  "author": "fafayzf <fafayzf@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@yzfu/bumpx": "^1.2.0",
    "esbuild": "^0.19.3"
  }
}
