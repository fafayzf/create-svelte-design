import cac from 'cac'
import minimist from 'minimist'

export async function create() {
  try {
    const cli = cac('create-svelte-design')
   
    
  } catch (e) {
    console.error(e)
    process.exit(1)
  }
}